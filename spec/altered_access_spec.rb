# frozen_string_literal: true

RSpec.describe AlteredAccess do
  it "has a version number" do
    expect(AlteredAccess::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
